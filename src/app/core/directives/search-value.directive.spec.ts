import { SearchValueDirective } from './search-value.directive';

describe('SearchValueDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchValueDirective();
    expect(directive).toBeTruthy();
  });
});
